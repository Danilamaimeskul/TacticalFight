import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  statusBlock: {
    width: 120,
    marginTop: 10,
    marginHorizontal: 10,
  },
  image: {
    height: 110,
    width: 120,
  },
});

export default styles;
